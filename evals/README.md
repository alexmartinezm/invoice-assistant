# Evals

- `InvoiceAssistant.Evals/` — xUnit harness (F3, pending implementation).
- `cases/` — declarative YAML cases. Format and categories in [`docs/ai/evaluation.md`](../docs/ai/evaluation.md).

The two included cases act as the format contract for the harness: one read case (`read-overdue-01`) and one injection case (`injection-03`).
