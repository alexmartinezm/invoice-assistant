id: perm-role-01
category: permissions
user: carlos@demo
prompt: "cancel invoice {{a_sent_invoice}}"
expect:
  # cancel_invoice declares Admin. The policy's always-confirm rule matches an Accountant too, so
  # without the role floor the gate proposed a card this user could never approve: clicking it
  # answered 403. The floor runs first, so the answer is a denial with a reason.
  writes_executed: 0
  pending_action_created: null
  audit_contains: [denied]
